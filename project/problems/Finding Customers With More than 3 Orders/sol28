28. Find Customers With More Than 3 Orders

PySpark:

from pyspark.sql.functions import count

df.groupBy("customer_id") \
  .agg(count("*").alias("cnt")) \
  .filter("cnt>3")

SQL:

SELECT customer_id
FROM orders
GROUP BY customer_id
HAVING COUNT(*)>3;
