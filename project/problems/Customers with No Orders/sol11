11. Find Customers With No Orders

PySpark:

customer.join(
    orders,
    "customer_id",
    "left_anti"
)

SQL:

SELECT c.*
FROM customers c
LEFT JOIN orders o
ON c.customer_id=o.customer_id
WHERE o.customer_id IS NULL;
